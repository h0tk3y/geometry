dependencies {
    compile project(':geometry')
    runtime project(':geometry')
    testCompile project(':visualizer')
}
jar.archiveName = 'algorithms.jar'
jar.extension = 'algorithms'
jar.baseName = 'algorithms'
archivesBaseName = 'algorithms'